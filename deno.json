{
  "compilerOptions": {
    "lib": ["dom", "deno.ns", "deno.unstable"]
  },
  "tasks": {
    "install": "deno install --allow-all https://deno.land/x/wholemeal@0.0.16/main.ts",
    "test": "deno test --allow-all",
    "build": "wholemeal",
    "build-docs": "wholemeal --out=./docs/dist",
    "dev": "wholemeal --dev=true --out=./docs/dist",
    "dev-dev": "wholemeal-dev --dev=true --out=./docs/dist",
    "prepublish": "deno run --allow-write --allow-env --allow-read ./create-package.ts"
  },
  "lint": {
    "rules": {
      "exclude": ["no-case-declarations"]
    }
  }
}
