<script>
  import * as spec from "../spec.ts";
  export const name = "l-container";
  export const props = { flush: false };
</script>
<style>
  :host {
    display: block;
    margin: 0 auto;
  }

  @if !self.props.flush {
    @for const size in spec.screen_sizes {
      @media ":spec.screen_sizes[size].query" {
        :host {
          max-width: ":spec.screen_sizes[size].width";
        }
      }
    }
  }
</style>

<slot></slot>
